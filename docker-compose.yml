services:
  vulnbank:
    build: .
    container_name: novapay-api
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=sqlite:///./bank.db
      - JWT_SECRET=secret
      - DEBUG=true
    restart: unless-stopped
